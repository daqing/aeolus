<?php
  /**
   * File cache settings
   *
   */
  
  # Directory where cache data was stored
  define('FC_DIR',AEOLUS_ROOT.'/tmp/');

  /**
   * Default lifetime for valid cache
   *
   * Usually 360 seconds would be OK
   *
   */
  define('FC_LIFETIME',360);

  /**
   * Cache directory level
   *
   * Usually one level would be OK
   *
   */
  define('FC_HASHLEVEL',1);

?>
  define('FC_HASHLEVEL',1);
